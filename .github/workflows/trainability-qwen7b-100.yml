name: Trainability Qwen2.5 7B 100-Step Backup
on:
  push:
    paths:
      - '.github/workflows/trainability-qwen7b-100.yml'
  workflow_dispatch:
permissions:
  contents: read
env:
  HF_HOME: ${{ github.workspace }}/.hf-cache
  HF_HUB_DOWNLOAD_TIMEOUT: '180'
  HF_HUB_ETAG_TIMEOUT: '30'
  TOKENIZERS_PARALLELISM: 'false'
  PYTHONUNBUFFERED: '1'
jobs:
  long100:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Free disk
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc || true
          df -h
      - name: Install CPU stack
        run: |
          python -m pip install --upgrade pip
          pip install --extra-index-url https://download.pytorch.org/whl/cpu torch
          pip install 'transformers>=4.51,<5' 'huggingface_hub>=0.34' safetensors accelerate sentencepiece protobuf hf_xet 'datasets>=3,<5'
      - name: Run 7B 100-step full-LM continuation
        shell: bash
        run: |
          set -euo pipefail
          d='results/qwen7b-long-seed2'; mkdir -p "$d"
          python trainability-real-llm/qwen7b_long_continuation.py --seed 2 --steps 100 --out "$d" 2>&1 | tee "$d/run.log"
          test -n "$(find "$d" -name '*.json' -print -quit)"
      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: tq-qwen7b-100-seed2
          path: results/qwen7b-long-seed2/
          if-no-files-found: error
          retention-days: 30
