The complete raw result set is stored in `raw_results.tar.gz`.

From the `vanish/` directory run:

```bash
tar -xzf results/raw/raw_results.tar.gz
```

This restores the four run-level files used by the analysis scripts.
